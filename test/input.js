/**
 * A test class
 *
 * @example
 * import Test from 'test';
 * const test = new Test({ a: true });
 *
 * @param {Object} options - The options.
 * @param {Boolean} [options.a=true] - An option.
 */
class Test {
  constructor(options) {

  }

  /**
   * Say hello method
   * @param {String} str - The string to say.
   */
  sayHello(str) {

  }

  /**
   * Say hello alternative method
   * @param {String} str - The string to say.
   */
  sayHello2(str) {

  }
}

export default Test
